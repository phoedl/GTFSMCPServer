name: gtfs-mcp-server
metadata:
  template: containerapp@0.0.1-beta
infra:
  provider: bicep
  path: infra
services:
  gtfs-mcp-server-prod:
    project: GTFSMCPServer
    host: containerapp
    language: dotnet
    docker:
      path: Dockerfile
      context: ./
